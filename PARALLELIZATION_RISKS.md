# 멀티 에이전트/병렬 처리 확장 — 위험 요소 분석

작성일: 2026-09-16
목적: "영상/사진/기획/검수를 각자 역할로 병렬 처리, 나중엔 여러 주제(계정)를 동시에" 요청에 대해,
실제 코드를 확인해 위험 요소를 정확히 파악한다. **이 문서는 분석 전용이며 아직 아무 코드도 바꾸지 않았다.**

---

## 0. 요청을 두 가지로 나눠서 봐야 하는 이유

사용자의 요청은 실제로 규모가 다른 두 가지 병렬화를 포함한다. 위험 수준이 완전히 다르므로 나눠서 접근한다.

| | A. 한 계정 안에서 여러 주제를 동시 처리 | B. 계정(주제 카테고리) 자체를 여러 개 동시 운영 |
|---|---|---|
| 예시 | 오늘의 여행 주제 3개를 동시에 이미지/영상/문구 생성 | Land in Korea + 나중에 만들 3개 이상의 다른 주제 계정을 동시에 자동 실행 |
| 데이터 공유 여부 | **공유함** — 같은 `data/queue.json`, `data/topic_state.json` | **공유 안 함** — 계정마다 별도 저장소, 별도 데이터 파일 (`dashboard/lib/accounts.js` 구조가 이미 이렇게 설계됨) |
| 위험 수준 | 중간 — 파일 동시쓰기 경쟁 가능성 직접 확인 필요 | 낮음(데이터 자체는) + API 비용/자원 경합 등 운영상 리스크 |

---

## 1. A: 한 계정 안에서 여러 주제 동시 처리 — 실제로 확인한 것

### 1-1. 지금 순차적인 지점 (`lib/scheduler/queue_topic.js`)

`queueOneTopic()` 안에서 아래가 전부 순서대로 `await`로 기다린다:
1. `resolveImages()` (Pexels/Pixabay/Odii 이미지 검색)
2. `findKoreaVideo()` / Pixabay 영상 검색
3. `curateContent()` (문구 생성 — Claude 또는 템플릿)

**이 세 가지는 서로 결과를 필요로 하지 않는다** (문구 생성은 이미지/영상과 무관, 이미지와 영상도 서로 무관). 이 부분만 `Promise.all`로 묶으면 한 주제 처리 시간이 줄어든다 — 이건 안전하다(아래 1-2에서 그 이유를 검증함).

### 1-2. 공유 상태 파일이 동시쓰기에 실제로 안전한지 직접 확인함

- `lib/scheduler/topic_rotation.js`의 `recordImageUrl`/`recordVideoUrl`/`recordMusicUrl`/`pickNextTopic` — grep으로 확인: **내부에 `await`가 하나도 없다.** `fs.readFileSync`로 읽고 `fs.writeFileSync`로 쓰는 완전한 동기 함수다.
- `lib/scheduler/queue.js`의 `addPost()`도 마찬가지로 완전 동기 함수다.

Node.js는 싱글 스레드라 **완전히 동기적인 함수는 실행 도중에 다른 비동기 작업이 끼어들 수 없다** — 함수가 시작되면 끝날 때까지 한 번에 실행된다. 그래서:

- 주제 A와 주제 B를 `Promise.all`로 "동시에" 처리해도, `recordImageUrl()` 호출 자체는 각각 원자적으로 끝까지 실행된다.
- A가 먼저 끝나서 상태를 저장하면, B가 나중에 `recordImageUrl()`을 호출할 때는 `loadState()`가 A가 저장한 최신 상태를 다시 읽어온다.
- 결과적으로 **지금 코드는 "우연히" 안전하다** — lost-update(덮어쓰기로 기록이 사라지는 현상)가 일어나지 않는다.

### 1-3. 그런데도 위험 요소로 꼭 적어야 하는 이유

이 안전성은 **암묵적이고 깨지기 쉽다**:

- 누군가 나중에 성능/원격 저장(예: 상태를 로컬 파일 대신 DB나 API로 옮기기)을 이유로 `recordImageUrl` 안에 `await`를 하나라도 넣는 순간, 이 안전성은 조용히 깨진다. 에러가 나지 않고 그냥 가끔 이미지가 중복되기 시작한다 — 디버깅하기 가장 어려운 종류의 버그다.
- `ffmpeg`를 쓰는 `attachMusicToVideo`(`lib/media/mix_audio.js`)는 CPU를 많이 쓰는 외부 프로세스 호출이다. 여러 주제를 동시에 처리하면 ffmpeg 프로세스가 동시에 여러 개 떠서 로컬 머신 CPU/메모리를 두고 경쟁한다 — 데이터 안전성 문제는 아니지만, 동시 처리 개수를 무제한으로 두면 안 되는 실질적 이유다.
- `dashboard/lib/git_sync.js`의 `syncAndCommit`(pull→수정→commit→push)은 **같은 계정 저장소에 대해 동시에 두 번 실행되면 위험하다** — git 프로세스 두 개가 같은 워킹 디렉터리를 동시에 건드리면 인덱스가 꼬일 수 있다. 지금은 대시보드 버튼을 한 번에 하나만 누른다는 가정에 의존하고 있다(잠금 장치 없음).

### 1-4. 권장 순서 (A 범위)

1. **1단계(안전, 바로 가능)**: 한 주제 안에서 이미지/영상/문구 생성만 `Promise.all`로 병렬화. 회귀 테스트: 병렬화 전/후로 같은 시드에 대해 동일한 이미지·영상·문구가 나오는지 비교(순서만 바뀌고 결과는 같아야 함).
2. **2단계(제한된 병렬, 신중)**: 하루 3개 주제를 동시 처리 — 단, 동시 실행 개수를 상수로 제한(예: 최대 3개 — 지금 하루 처리량과 동일해서 위험이 늘지 않음)하고, ffmpeg 동시 실행 개수도 별도로 제한.
3. **3단계**: `git_sync.js`에 파일 기반 잠금(lock 파일 하나 두고, 이미 잠겨 있으면 대기 또는 거부) 추가 — 대시보드에서 버튼 연타/여러 탭 동시 조작을 실제로 막음.

---

## 2. B: 계정(주제) 자체를 여러 개 동시 운영 — 실제로 확인한 것

### 2-1. 데이터는 이미 완전히 격리되어 있다

`dashboard/lib/accounts.js`는 계정마다 `repoPath`(독립된 저장소 경로)를 갖고, `dashboard/lib/data.js`/`git_sync.js`/`runner.js`는 전부 그 `repoPath` 기준으로 동적 `require()`/`fs` 경로를 계산한다. 즉 계정 A와 계정 B는:
- `data/queue.json`, `data/topic_state.json`이 **서로 다른 파일**(다른 저장소)
- `.env`(API 키, SNS 토큰)도 **서로 다른 파일**
- git 저장소 자체가 다름

**그래서 계정 간에는 1장에서 다룬 파일 동시쓰기 문제가 원천적으로 없다.** 이미 안전하게 설계돼 있다.

### 2-2. 그럼에도 실제로 막아야 하는 위험 요소 (전부 코드에서 확인함)

1. **미디어 호스팅 저장소가 하드코딩돼 있다.** `lib/scheduler/queue_topic.js` 안에서 `uploadMediaFile('solusupport-bot/desktop-tutorial', ...)`, `watermarkAndHostImages(images, 'solusupport-bot/desktop-tutorial', ...)`처럼 **저장소 이름이 문자열로 박혀 있다.** 새 계정을 이 파일을 복제해서 만들 때 이 문자열을 그 계정 자신의 저장소로 바꾸지 않으면, 새 계정이 Land in Korea 저장소의 media-assets 브랜치에 몰래 이미지를 계속 업로드하게 된다 — 저장 공간 오염 + 두 계정 이미지가 한 저장소에 섞임.
2. **댓글 자동응답 서비스(`services/comment-auto-reply`)는 지금 계정 1개 전용으로 배포돼 있다.** Meta 웹훅과 지식베이스(KNOWLEDGE_BASE)가 Land in Korea 콘텐츠에 고정돼 있어, 새 계정의 IG/FB 페이지 댓글에 그대로 쓸 수 없다. 계정마다 별도 배포하거나, 페이지 ID로 분기하는 구조로 바꿔야 한다(지금은 안 돼 있음).
3. **API 비용이 계정 수만큼 그대로 곱해진다.** Claude/Pexels/Pixabay 호출은 계정마다 독립적으로 일어나므로, 계정 3개를 동시에 돌리면 비용도 3배. 계정별 일일 상한(예: 지금처럼 하루 3주제)을 유지하는 한 폭증하지는 않지만, 계정 수가 늘면 총비용은 선형으로 늘어난다는 점을 예산에 반영해야 한다.
4. **GitHub Actions는 계정별로 이미 격리된다(저장소가 다르므로).** 이건 문제가 아니라 오히려 장점 — 계정마다 자기 저장소의 Actions 분/무료한도를 따로 쓴다. 다만 로컬 머신에서 대시보드로 "지금 실행" 버튼을 여러 계정에 대해 동시에 누르면, 3장에서 말한 ffmpeg/git 경합이 계정 수만큼 겹쳐서 일어날 수 있다.
5. **비밀키(.env) 혼동.** 계정마다 자기 저장소 안에 자기 `.env`를 두는 구조라 원칙적으로는 안전하지만, 사람이 실수로 계정 B의 저장소에 계정 A의 `.env`를 복사해 넣으면 계정 B의 콘텐츠가 계정 A의 SNS 계정에 발행되는 사고가 날 수 있다 — 코드가 막아주는 게 아니라 운영 절차(체크리스트)로 막아야 하는 부분.

### 2-3. 권장 순서 (B 범위)

1. 새 계정을 만들 때 반드시 확인할 체크리스트를 문서화(아래 4장) — 특히 하드코딩된 저장소 문자열 교체.
2. 계정별 `.env`가 실제로 그 계정 자신의 자격증명인지 확인하는 아주 작은 스타트업 점검 스크립트(예: 토큰으로 "내 계정 정보 조회" API를 한 번 불러 실제 계정명이 기대한 이름과 일치하는지 확인) — 아직 없음, 필요하면 다음 단계에서 만들 것.
3. 댓글봇은 계정이 2개 이상 되는 시점에 반드시 다시 설계(계정별 배포 또는 페이지ID 분기) — 지금 1개 계정 상태에서는 급하지 않음.
4. 로컬에서 여러 계정을 동시 실행할 계획이면 2-2-4의 자원 경합을 고려해 동시 실행 개수를 제한.

---

## 3. "다른 AI를 붙여도 된다"는 부분 — 지금 구조에서 어떻게 들어갈 수 있는지

- 이미 `package.json`에 `@anthropic-ai/sdk`와 `@google/generative-ai`(Gemini) 둘 다 의존성으로 있다. 다만 실제로 코드에서 쓰는 건 Claude(`lib/ai/claude.js`)뿐이고 Gemini는 현재 어디서도 호출되지 않는다.
- "역할별로 다른 AI"를 붙이려면 자연스러운 지점은 이미지 선정/영상 선정처럼 지금 규칙 기반(밝기/대비 점수 등)으로 하는 부분에 비전 모델을 추가하는 것, 또는 문구 생성에서 Claude/Gemini를 A/B로 나누는 것 정도다. 이건 순수 추가라 안전하지만, 아직 구체적으로 "어느 역할에 어느 모델"인지 정해진 게 없어 지금은 설계만 짚어두고 실제 구현은 다음 단계로 미룬다.

---

## 4. 새 계정을 추가할 때 실수 방지 체크리스트 (2-2-1, 2-2-5 대응)

- [ ] `lib/scheduler/queue_topic.js` 안의 `'solusupport-bot/desktop-tutorial'` 문자열 전부(`uploadMediaFile`, `watermarkAndHostImages` 호출부)를 그 계정 자신의 저장소로 교체했다.
- [ ] 새 계정 저장소의 `.env`에 들어있는 SNS 토큰이 실제로 그 계정의 페이지/프로필 것인지(다른 계정 것을 복사한 게 아닌지) 확인했다.
- [ ] `dashboard/lib/accounts.js`에 등록한 `repoPath`/`blogRepoPath`가 정확히 그 새 계정 저장소를 가리키는지 확인했다.
- [ ] 로컬에서 여러 계정을 동시에 "지금 실행"할 계획이면, 한 번에 실행하는 계정 수를 의도적으로 제한했다(무제한 동시 실행 금지).

---

## 5. 결론 — 지금 당장 안전하게 할 수 있는 것 / 나중에 설계가 더 필요한 것

**지금 바로 안전하게 진행 가능:**
- 한 주제 안에서 이미지/영상/문구 생성 병렬화 (1-4의 1단계)
- 하루 3개 주제를 제한된 동시성으로 병렬화 (1-4의 2단계)

**설계를 더 해야 안전한 것:**
- 계정(주제) 자체를 여러 개 동시 운영 — 데이터는 이미 안전하지만, 하드코딩된 미디어 저장소 문자열 정리, 댓글봇 다중 계정 대응, 자원 경합 제한이 먼저 필요.
- `git_sync.js`의 동시 실행 잠금 — 지금은 없음.

이 문서에서는 코드를 변경하지 않았다. 다음 단계로 진행할 항목을 알려주면 그때 구현한다.
